<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_login"
    tools:context=".Fragment.ForgotFragment">

    <!-- TODO: Update blank fragment layout -->


    <ImageView
        android:id="@+id/iv_logo_forgot"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/ll_input_forgot"
        android:layout_marginBottom="@dimen/space_30dp"
        android:src="@drawable/logo" />

    <LinearLayout
        android:id="@+id/ll_input_forgot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <EditText
            android:id="@+id/et_username_forgot"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_person_sky_blue_24dp"
            style="@style/LoginInput"
            android:hint="@string/username" />


        <EditText
            android:id="@+id/et_email_forgot"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_email_sky_blue_24dp"
            style="@style/LoginInput"
            android:hint="@string/email"
            />
    </LinearLayout>
    <Button
        android:id="@+id/btn_login_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ll_input_forgot"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_button_login"
        android:paddingLeft="@dimen/space_30dp"
        android:paddingTop="@dimen/space_10dp"
        android:paddingRight="@dimen/space_30dp"
        android:paddingBottom="@dimen/space_10dp"
        android:text="@string/get_password"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="@dimen/font_size_18sp" />

</RelativeLayout>